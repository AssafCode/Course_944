<Window x:Class="HelloWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
        Title="MainWindow" Height="450" Width="800">

   
    
    <StackPanel Name="mainPanel">
        <Button Content="OK" Click="Button_Click" Background="Yellow"/>
        <Button Content="OK" Click="Button_Click" Background="Yellow"/>

        <StackPanel>
            <Ellipse Height="100" Width="100" Fill="Tan"/>
            <!--optins to crate button with text content-->
            <Button Height="30" Width="30" Background="LightBlue">
                <Button.Content>
                    <TextBlock>OK1</TextBlock>
                </Button.Content>  
            </Button>
            <Button Background="LightCoral" Height="30" Width="30">Ok2</Button>
            <Button Content="Ok3"  Background="DarkOliveGreen" Height="30" Width="30"/>
        </StackPanel>
      
        <!--<Button>
            <Label>
                <CheckBox>
                    <Button>OK</Button>
                </CheckBox>
            </Label>
        </Button>-->
    </StackPanel>
</Window>
