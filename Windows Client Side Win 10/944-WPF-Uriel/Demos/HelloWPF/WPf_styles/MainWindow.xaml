<Window x:Class="WPf_styles.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WPf_styles"
        Title="MainWindow" Height="450" Width="800">

    <Grid Background="{StaticResource myColor}">
       
        <StackPanel>
           <!-- <StackPanel.Background>
                <RadialGradientBrush>
                    <GradientStop Color="Black" Offset="0"/>
                    <GradientStop Color="White" Offset="1"/>
                </RadialGradientBrush>
            </StackPanel.Background>
            <TextBlock Text="{StaticResource i}" />
            <Ellipse Style="{StaticResource myStyle}"/>
            <Ellipse/>
            <Ellipse/>
            <Ellipse/>
            <ContentControl x:Name="background" Content="{StaticResource elps1}"  />
            <ContentControl Content="{StaticResource elps1}" />-->



            <!--<Rectangle Height="100" Width="100" Fill="{StaticResource myColor2}" />-->
            <Rectangle Height="100" Width="100" StrokeThickness="13" Stroke="Red" Fill="{DynamicResource myColor2}" />
            <Button Content="Add Dynamic color (myColor2)" Click="Button_Click_2" />
        </StackPanel>
    </Grid>
</Window>
